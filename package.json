{
  "scripts": {
    "prefix": "postcss ./style.css --use autoprefixer -d ./css/ --watch"
  },
  "browserslist": [
    "defaults"
  ],
  "dependencies": {
    "autoprefixer": "^10.4.4",
    "postcss": "^8.4.12",
    "postcss-cli": "^9.1.0"
  }
}
